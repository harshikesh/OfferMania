package com.message.aim.utils;

/**
 * Created by harshikesh.kumar on 10/09/16.
 */
public class Waste {


   /*     String str = "SMS From: " + smsInboxCursor.getString(date) +
          "\n" + smsInboxCursor.getString(indexAddress) +
          "\n" + smsInboxCursor.getString(indexBody) +
          "\n " + smsInboxCursor.getString(threadId) +
          " :: " + smsInboxCursor.getString(seen) +
          " :: " + smsInboxCursor.getString(type) +
          " :: " + smsInboxCursor.getString(subject) +
          " :: " + smsInboxCursor.getString(person);
*/


  //contentResolver.query(Uri.parse("content://sms/inbox"),
  //    new String[] { "_id", "thread_id", "address", "person", "date", "body", "type" }, null,
  //    null, null);
  //if (smsInboxCursor.moveToFirst()) { // must check the result to prevent exception
  //  do {
  //    String msgData = "";
  //    for (int idx = 0; idx < smsInboxCursor.getColumnCount(); idx++) {
  //      msgData += " " + smsInboxCursor.getColumnName(idx) + ":" + smsInboxCursor.getString(idx);
  //    }
  //    // use msgData
  //  } while (smsInboxCursor.moveToNext());
  //} else {
  //  // empty box, no SMS
  //}

}
